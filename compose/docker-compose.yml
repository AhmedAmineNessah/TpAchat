# docker-compose.yaml
  version: '3.8'
  services:
    jenkins:
      image: jenkinsci/blueocean:latest
      ports:
       - 8080:8080
      - 50000:50000
    container_name: myjenkins
    volumes:
      - /home/vagrant/jenkins:/var/jenkins_home
      
